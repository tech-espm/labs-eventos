<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="pt-br">
<head>
	<meta charset="utf-8" />
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />

	<title><%=((typeof titulo === "undefined" || !titulo) ? "Controle de Eventos" : (titulo + " - Controle de Eventos")) %></title>

	<%- include("cabecalho") %>

	<style type="text/css">
		html {
			min-height: 100%;
			height: 100%;
		}

		body {
			background-image: url(/public/imagens/loginbg.jpg);
			background-size: cover;
			background-repeat: no-repeat;
			background-position: center;
			background-attachment: fixed;
			min-height: 100%;
		}

		#content {
			margin: 30px 0 15px;
		}

		#page-wrapper {
			margin: 0;
		}

			#page-wrapper > .row {
				margin: 0 auto;
				max-width: 800px;
			}

		.navbar-header {
			float: left;
		}

		.navbar-right {
			float: right;
		}

		.panel {
			-webkit-box-shadow: 0 2px 8px rgba(0,0,0,.5);
			box-shadow: 0 2px 8px rgba(0,0,0,.5);
		}

			.panel .panel {
				-webkit-box-shadow: 0 1px 4px rgb(0,0,0,0.15);
				box-shadow: 0 1px 4px rgb(0,0,0,0.15);
			}

		.btn {
			white-space: normal;
		}

		.badge-sala {
			font-size: inherit;
			white-space: normal;
		}

		.painel-msg {
			/*<% if ((typeof panelHeadingPersonalizado !== "undefined") && panelHeadingPersonalizado) { %>*/
			text-align: left;
			padding: 50px 15px;
			/*<% } else { %>*/
			text-align: center;
			padding: 10% 15px 15px;
			/*<% } %>*/
			max-width: 600px;
			margin: 0 auto;
		}

		.painel-msg h1, painel-msg h2 {
			margin: 15px;
		}
	</style>
	<% if ((typeof panelHeadingPersonalizado !== "undefined") && panelHeadingPersonalizado) { %>
	<style type="text/css">
		.panel-default > .panel-heading {
			background-color: rgba(255,255,255,0.85);
			font-size: 25px;
		}

			.panel-default > .panel-heading span {
				font-size: 40px;
			}
	</style>
	<% } %>

	<%- defineContent("styles") %>
</head>
<body>

	<%- include("navbar-participante") %>

	<div id="page-wrapper">
		<%- body %>
	</div>

	<%- include("rodape") %>

	<%- defineContent("scripts") %>
</body>
</html>
