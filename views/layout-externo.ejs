<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="pt-br">
<head>
	<meta charset="utf-8" />
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1" />

	<title><%=((typeof titulo === "undefined" || !titulo) ? "Controle de Eventos" : (titulo + " - Controle de Eventos")) %></title>

	<%- include("cabecalho") %>

	<% if ((typeof imagemFundo !== "undefined") && imagemFundo) { %>
	<style type="text/css">
		html {
			min-height: 100%;
			height: 100%;
		}

		body {
			background-image: url(/imagens/loginbg.jpg);
			background-size: cover;
			background-repeat: no-repeat;
			background-position: center;
			min-height: 100%;
		}

		.panel {
			-webkit-box-shadow: 0 2px 8px rgba(0,0,0,.5);
			box-shadow: 0 2px 8px rgba(0,0,0,.5);
		}

		h1 {
			text-align: center;
			margin: 0;
		}

		.btn {
			white-space: normal;
		}
	</style>
	<% } %>

	<%- defineContent("styles") %>
</head>
<body>

	<%- body %>

	<%- include("rodape") %>

	<%- defineContent("scripts") %>
</body>
</html>
